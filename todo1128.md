# 2025-11-28 工作清單

## 專案：HealthCalculator (InBody 分析代理人)

### 已完成事項

- [x] **了解需求**
  - 閱讀並理解 User Story、Prototype、Milestone 及 Gantt Chart。
  - 確認功能需求：BMI、BMR、TDEE 計算。

- [x] **設計 API**
  - 定義資料模型 (Models)：`BmiRequest`, `BmiResponse`, `BmrRequest`, `BmrResponse`, `TdeeRequest`, `TdeeResponse`。
  - 定義列舉 (Enums)：`Gender`, `ActivityLevel`。
  - 設計介面 (Interfaces)：`IBmiService`, `IBmrService`, `ITdeeService`。

- [x] **實作 API (ASP.NET Core 9)**
  - 建立 Web API 專案 (`dotnet new webapi`)。
  - 實作服務邏輯 (Services)：
    - `BmiService`: 計算 BMI 與判斷體位。
    - `BmrService`: 使用 Mifflin-St Jeor 公式計算 BMR。
    - `TdeeService`: 結合 BMR 與活動量係數計算 TDEE。
  - 設定依賴注入 (DI) 與 Minimal API 端點 (`/health/bmi`, `/health/bmr`, `/health/tdee`)。
  - 設定 JSON Enum 轉換器。

- [x] **設計前端 (Razor Pages + Bootstrap 5)**
  - 設定專案支援 Razor Pages。
  - 建立共用版面配置 (`_Layout.cshtml`) 並引入 Bootstrap 5。
  - 設計首頁 (`Index.cshtml`) 表單介面 (性別、年齡、身高、體重、活動量)。

- [x] **實作前端邏輯 (JavaScript)**
  - 實作表單輸入監聽，動態啟用/停用計算按鈕。
  - 使用 `fetch` 非同步呼叫後端 API。
  - 處理並顯示計算結果。

- [x] **實作資料儲存與載入**
  - 使用 `localStorage` 儲存使用者輸入的身體資訊。
  - 頁面載入時自動回填上次的資料。

- [x] **介面繁體中文化**
  - 將所有 UI 標籤、選項、按鈕及提示訊息翻譯為繁體中文。

### 備註
- 專案路徑：`c:\Shares\Demo05\HealthCalculator`
- 執行方式：`dotnet run`
